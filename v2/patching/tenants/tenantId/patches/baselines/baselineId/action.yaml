post:
  summary: Activate or deactivate patch baseline
  description: This endpoint is used to activate or deactivate a patch baseline.
  tags:
    - patching
  responses:
    '200':
      description: OK
parameters:
  - $ref: '../components-global.yaml#/components/parameters/tenantId'
  - schema:
      type: string
    in: path
    name: baselineId
    required: true
  - schema:
      type: string
    in: path
    name: action
    required: true
