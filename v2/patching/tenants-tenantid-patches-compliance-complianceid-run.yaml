summary: Run patch compliance check
description: Runs a patch compliance, checking all devices and device groups for compliance.
post:
  tags:
    - patching
  responses:
    '200':
      description: OK
parameters:
  - $ref: '../components-global.yaml#/components/parameters/tenantId'
  - schema:
      type: string
    in: path
    name: complianceId
    required: true
