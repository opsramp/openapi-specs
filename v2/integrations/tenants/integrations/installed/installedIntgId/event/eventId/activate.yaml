post:
  summary: Activate integration event
  description: This endpoint is used to activate an integration event.
  tags:
    - integrations
  responses:
    '200':
      description: OK
parameters:
  - $ref: '../components-global.yaml#/components/parameters/tenantId'
  - schema:
      type: string
    in: path
    name: installedIntgId
    required: true
  - schema:
      type: string
    in: path
    name: eventId
    required: true
