post:
  summary: Disable integration
  description: This endpoint is used to disable an installed integration and will temporarily disable its services.
  tags:
    - integrations
  responses:
    '200':
      description: OK
parameters:
  - schema:
      type: string
    in: path
    name: tenantId
    required: true
  - schema:
      type: string
    in: path
    name: installedIntgId
    required: true
