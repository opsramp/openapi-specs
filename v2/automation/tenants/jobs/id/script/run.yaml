post:
  summary: Run script
  description: |-
    This endpoint is used to run a script.

    Define scripts as code that can run commonly-run tasks.

    This API can be used to run an existing job script.
  tags:
    - automation
  responses:
    "200":
      description: OK
parameters:
  - $ref: '../components-global.yaml#/components/parameters/tenantId'
  - schema:
      type: string
    in: path
    name: id
    required: true
