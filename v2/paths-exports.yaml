paths:
  api.opsramp.com/api/v2/exports/tenants/{tenantId}/create:
    post:
      summary: Create on-demand batch export
      description: |-
        This endpoint is used to create an on-demand batch export template.

        ## Sample URLs
        ```
        
        ```
        ## Sample cURL
        ```
        
        ```
      operationId: exports-create-post/
      tags:
        - exports
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
              examples:
                example-1:
                  value:
      requestBody:
        content:
          application/json:
            schema:
            examples:
              example-1:
      externalDocs:
        url: https://docs.opsramp.com/api/exports/exports-create-post/
    post2:
      summary: Create monthly recurring batch export
      description: |-
        This endpoint is used to create monthly recurring batch export template.

        ## Sample URLs
        ```
        
        ```
        ## Sample cURL
        ```
        
        ```
      operationId: exports-recurring-monthly-post/
      tags:
        - exports
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
              examples:
                example-1:
                  value:
      requestBody:
        content:
          application/json:
            schema:
            examples:
              example-1:
      externalDocs:
        url: https://docs.opsramp.com/api/exports/exports-recurring-monthly-post/
    parameters:
      - schema:
          type: string
        in: path
        name: tenantId
        required: true
  /api/v2/exports/tenants/{tenantId}/create:
    post:
      summary: Create daily recurring batch export
      description: |-
        This endpoint is used to create daily recurring batch export template.

        ## Sample URLs
        ```
        
        ```
        ## Sample cURL
        ```
        
        ```
      operationId: exports-recurring-daily-post/
      tags:
        - exports
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
              examples:
                example-1:
                  value:
      requestBody:
        content:
          application/json:
            schema:
            examples:
              example-1:
      externalDocs:
        url: https://docs.opsramp.com/api/exports/exports-recurring-daily-post/
    post2:
      summary: Create weekly recurring batch export
      description: |-
        This endpoint is used to create weekly recurring batch export template.

        ## Sample URLs
        ```
        
        ```
        ## Sample cURL
        ```
        
        ```
      operationId: exports-recurring-weekly-post/
      tags:
        - exports
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
              examples:
                example-1:
                  value:
      requestBody:
        content:
          application/json:
            schema:
            examples:
              example-1:
      externalDocs:
        url: https://docs.opsramp.com/api/exports/exports-recurring-weekly-post/
    parameters:
      - schema:
          type: string
        in: path
        name: tenantId
        required: true
