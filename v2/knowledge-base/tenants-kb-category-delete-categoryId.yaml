summary: Delete KB categories
description: This endpoint is used to delete a knowledge base category.
delete:
  tags:
    - knowledge-base
  responses:
    '200':
      description: OK
parameters:
  - $ref: '../components-global.yaml#/components/parameters/tenantId'
  - schema:
      type: string
    in: path
    name: categoryId
    required: true
