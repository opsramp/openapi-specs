delete:
  summary: Delete template
  description: This endpoint is used to delete a knowledge base template.
  tags:
    - knowledge-base
  responses:
    '200':
      description: OK
parameters:
  - $ref: '../components-global.yaml#/components/parameters/tenantId'
  - schema:
      type: string
    in: path
    name: templateId
    required: true
